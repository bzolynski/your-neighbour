import { RelativeDirective } from './relative.directive';

describe('RelativeDirective', () => {
  it('should create an instance', () => {
    const directive = new RelativeDirective();
    expect(directive).toBeTruthy();
  });
});
