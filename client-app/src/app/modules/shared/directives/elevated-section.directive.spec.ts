import { ElevatedSectionDirective } from './elevated-section.directive';

describe('ElevatedSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ElevatedSectionDirective();
    expect(directive).toBeTruthy();
  });
});
