import { TreeViewOutletDirective } from './tree-view-outlet.directive';

describe('TreeViewOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeViewOutletDirective();
    expect(directive).toBeTruthy();
  });
});
