import { TreeViewNodeToggleDirective } from './tree-view-node-toggle.directive';

describe('TreeViewNodeToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeViewNodeToggleDirective();
    expect(directive).toBeTruthy();
  });
});
