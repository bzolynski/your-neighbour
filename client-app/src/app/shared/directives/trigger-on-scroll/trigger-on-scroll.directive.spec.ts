import { TriggerOnScrollDirective } from './trigger-on-scroll.directive';

describe('TriggerOnScrollDirective', () => {
    it('should create an instance', () => {
        const directive = new TriggerOnScrollDirective();
        expect(directive).toBeTruthy();
    });
});
