import { ParentWidthDirective } from './parent-width.directive';

describe('ParentWidthDirective', () => {
    it('should create an instance', () => {
        const directive = new ParentWidthDirective();
        expect(directive).toBeTruthy();
    });
});
