import { CardContainerDirective } from './card-container.directive';

describe('CardContainerDirective', () => {
    it('should create an instance', () => {
        const directive = new CardContainerDirective();
        expect(directive).toBeTruthy();
    });
});
